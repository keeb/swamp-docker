manifestVersion: 1
name: "@keeb/docker"
version: "2026.05.31.1"
description: "Manage Docker Engine and Docker Compose lifecycle on remote hosts over SSH, including install, build, run, inspect, exec, and compose service operations."
repository: https://github.com/keeb/swamp-docker
labels:
  - docker
  - containers
  - infrastructure
platforms: []
additionalFiles:
  - README.md
  - LICENSE.txt
models:
  - docker_engine.ts
  - docker_compose.ts
workflows:
  - setup-docker/workflow.yaml
skills:
  - docker
dependencies:
  - "@keeb/ssh"
